[project]
name = "gaugefixer-manuscript"
version = "0.1.0"
description = "Environment for reproducing the analyses in manuscript/code/"
requires-python = ">=3.10,<3.13"
dependencies = [
    "gaugefixer>=0.1.0",
    "logomaker>=0.8",
    "matplotlib>=3.8",
    "numpy>=1.26",
    "pandas>=2.0",
    "scipy>=1.11",
    "seaborn>=0.13",
]

[tool.uv]
package = false
